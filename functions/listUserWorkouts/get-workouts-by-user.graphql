query WorkoutsByUserId($user_id: String) {
  workouts(where: { user_id: { _eq: $user_id } }) {
    __typename
    created_at
    goal_per_day
    id
    interval
    name
    repeat
    rest
    squeeze
    stop_after
    type
    updated_at
    user_id
  }
}
